
# Copy to .env and set values, then deploy (Render/Railway/Fly/etc.)
GITHUB_TOKEN=ghp_xxx            # PAT with repo contents:read/write on the target repo
GITHUB_OWNER=your-github-user
GITHUB_REPO=your-data-repo      # create empty public or private repo first (e.g., chillfeed-data)
GITHUB_BRANCH=main
PORT=3000
